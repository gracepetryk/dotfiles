{
	"Debugpy start server": {
		"prefix": "debugpy",
		"body": [
			"import debugpy; debugpy.listen(('0.0.0.0', ${1|5678,7890|})) if not debugpy.is_client_connected() else None; print('waiting for client to attach'); debugpy.wait_for_client()"
		]
	}
}
