{
	"Debugpy start server": {
		"prefix": "debugpy",
		"body": [
			"import debugpy; debugpy.listen(('0.0.0.0', 5678)) if not debugpy.is_client_connected() else None; print('waiting for client to attach'); debugpy.wait_for_client()"
		]
	}
}
